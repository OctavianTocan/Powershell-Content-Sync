# =============================================================================
# Asset Sync Configuration
# =============================================================================
# This file contains project-specific settings for the sync-assets script.
# Edit these values to match your project setup.
#
# IMPORTANT: Do not commit this file if it contains sensitive information.
# Add .env to your .gitignore if needed.
# =============================================================================

# -----------------------------------------------------------------------------
# Local Path Configuration
# -----------------------------------------------------------------------------
# Relative path from this script's directory to your Content folder
# Use ".." to go up one directory level
# Example: "..\Content" means Content folder is in parent directory
ASSET_DESTINATION=..\Content

# -----------------------------------------------------------------------------
# Google Drive Configuration
# -----------------------------------------------------------------------------
# Name of your rclone remote (must match rclone.conf section name)
REMOTE_NAME=gdrive

# Path to your assets folder in Google Drive (no leading/trailing slashes)
REMOTE_FOLDER=MyProject/Content
# Path to rclone config file (relative to this script's directory)
RCLONE_CONFIG_PATH=.rclone\rclone.conf

# Name of the safety check file used by --check-access
# This file must exist in both source and destination
CHECK_FILENAME=RCLONE_TEST

# -----------------------------------------------------------------------------
# Sync Behavior Configuration
# -----------------------------------------------------------------------------
# Conflict resolution strategy when both sides modified the same file
# Options: newer, older, larger, smaller, path1, path2
CONFLICT_RESOLVE=newer

# What to do with the losing side of a conflict
# Options: num (rename with .conflict1, .conflict2), delete, pathname
CONFLICT_LOSER=num

# Maximum time to hold the bisync lock file (prevents permanent locks)
# Format: time with unit (e.g., 15m, 1h, 30s)
MAX_LOCK=15m

# File comparison method
# Options: size,modtime (fast), checksum (accurate but slow)
COMPARE=size,modtime
